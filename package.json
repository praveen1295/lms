{
  "name": "lms",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-server": "cd server && npm install && npm run build && npx puppeteer browsers install firefox",
    "start-server": "cd server && npm run start",
    "install-client": "cd admin && npm install --legacy-peer-deps",
    "build-client": "cd admin && npm run build",
    "start-client": "cd admin && npm run start"
  },
  "author": "praveen",
  "license": "ISC"
}
